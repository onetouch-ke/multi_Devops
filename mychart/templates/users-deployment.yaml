apiVersion: apps/v1
kind: Deployment
metadata:
  name: users
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
      - name: users
        image: "{{ .Values.backend.users.image }}:{{ .Values.backend.users.tag }}"
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.backend.users.port }}
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:mariadb://6fb21019-440b-4ad8-b69e-d9adc45c2c9b.internal.kr1.mariadb.rds.nhncloudservice.com/UsersService
        - name: SPRING_DATASOURCE_USERNAME
          value: "{{ .Values.mariadb.userName }}"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "{{ .Values.mariadb.rootPassword }}"
      imagePullSecrets:
      - name: ecr-secret
